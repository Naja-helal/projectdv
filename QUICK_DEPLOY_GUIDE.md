# 🚀 دليل Deploy السريع

## ✅ التعديلات الأخيرة (29 نوفمبر 2025):

### الميزات الجديدة:
1. ✅ **نظام إدارة العملاء** - صفحة كاملة لإدارة العملاء وربطهم بالمشاريع
2. ✅ **إزالة "نوع المشروع"** - استبدال بـ "تصنيف المشروع" (project_items)
3. ✅ **حفظ البيانات** - استخدام Railway Volume لعدم فقدان البيانات
4. ✅ **تحديثات تلقائية** - القاعدة تُحدث تلقائياً عند كل deploy

---

## 🎯 خطوات Deploy (بدون فقدان البيانات):

### 1️⃣ **إعداد Railway Volume** (مرة واحدة فقط):

في لوحة تحكم Railway:
- اذهب لمشروعك → Settings → Volumes
- اضغط **New Volume**
- الاسم: `expenses_data`
- Mount Path: `/app/data`
- احفظ

### 2️⃣ **Deploy التعديلات**:

```bash
# من المجلد الرئيسي
cd "c:\Users\naja2\Desktop\Tkamol2025\saud - Copy (4)"

# إضافة التعديلات
git add .

# Commit مع وصف
git commit -m "feat: نظام العملاء + حفظ البيانات + تحسينات"

# Push للـ main
git push origin main
```

### 3️⃣ **Railway سيعمل Deploy تلقائياً**:
- ✅ البناء والنشر التلقائي
- ✅ البيانات محفوظة في Volume
- ✅ التحديثات تُطبق على القاعدة

---

## 📊 ما الجديد في هذا الإصدار؟

### 🆕 نظام إدارة العملاء:
- **صفحة العملاء** (`/clients`):
  - إضافة/تعديل/حذف عملاء
  - بطاقات إحصائية (عدد العملاء، المشاريع، الميزانيات)
  - اختيار أيقونة ولون لكل عميل
  - بحث وفلترة وترقيم

- **صفحة تفاصيل العميل** (`/clients/:id`):
  - عرض معلومات العميل الكاملة
  - قائمة بجميع مشاريع العميل
  - إحصائيات العميل

- **ربط المشاريع بالعملاء**:
  - كل مشروع مرتبط بعميل
  - فلتر العملاء في صفحة المشاريع
  - فلتر العملاء في الإحصائيات
  - عرض اسم العميل مع المشروع

### 🔄 استبدال نوع المشروع:
- ❌ تم إزالة "نوع المشروع" نهائياً
- ✅ استخدام "تصنيفات المشروع" (`/project-items`)
- ✅ عرض اسم التصنيف مع أيقونته
- ✅ فلترة بالتصنيفات

### 💾 حفظ البيانات:
- ✅ قاعدة البيانات في Railway Volume
- ✅ لا يوجد فقدان للبيانات عند Deploy
- ✅ التحديثات التلقائية على القاعدة

---

## 🗂️ هيكل قاعدة البيانات:

### الجداول الرئيسية:
```
✅ clients              - العملاء (جديد)
✅ projects             - المشاريع (محدّث)
✅ project_items        - تصنيفات المشاريع
✅ expenses             - المصاريف الفعلية
✅ expected_expenses    - الإنفاق المتوقع
✅ categories           - تصنيفات المصاريف
✅ payment_methods      - طرق الدفع
✅ units                - الوحدات
```

### العلاقات:
```
clients (1) → (N) projects
projects (1) → (N) expenses
projects (1) → (N) expected_expenses
project_items (1) → (N) projects
```

---

## 🔧 متطلبات التشغيل:

### Backend (Railway):
- Node.js 18+
- Better-SQLite3
- Express.js
- Volume: `/app/data/`

### Frontend (Netlify):
- React 18
- Vite
- TypeScript
- TailwindCSS

---

## 📈 الإحصائيات:

### حجم البناء:
- **Frontend**: 1,021 KB (275 KB gzipped)
- **Backend**: 2.4 MB (compiled)

### عدد الملفات:
- 3,053 modules
- 615 lines (Clients page)
- 320 lines (ClientForm)

---

## 🎯 الصفحات الرئيسية:

```
/                    - Dashboard
/projects            - المشاريع
/clients             - العملاء (جديد)
/clients/:id         - تفاصيل العميل (جديد)
/expenses            - المصاريف الفعلية
/expected-expenses   - الإنفاق المتوقع
/statistics          - الإحصائيات
/charts              - الرسوم البيانية
/project-items       - تصنيفات المشاريع
/categories          - تصنيفات المصاريف
/payment-methods     - طرق الدفع
/units               - الوحدات
```

---

## 🔐 البيئة والمتغيرات:

### Backend `.env`:
```env
PORT=5175
NODE_ENV=production
DB_PATH=/app/data/expenses.db
JWT_SECRET=your-secret-key
ADMIN_USERNAME=admin
ADMIN_PASSWORD=your-password
```

### Frontend `.env`:
```env
VITE_API_URL=https://your-backend.railway.app
```

---

## 🚨 ملاحظات مهمة:

### ✅ افعل:
1. تأكد من إنشاء Volume قبل أول deploy
2. استخدم `git add .` و `git commit` و `git push`
3. انتظر اكتمال البناء في Railway
4. راجع الـ Logs للتأكد من النجاح

### ❌ لا تفعل:
1. لا تحذف الـ Volume من Railway
2. لا تغير `mountPath` بعد الإنشاء
3. لا تستخدم `DROP TABLE` بدون backup

---

## 📞 في حال المشاكل:

### مشكلة: البيانات ما زالت تُحذف
**الحل**: 
- تحقق من وجود Volume في Railway
- تحقق من `mountPath` = `/app/data`
- راجع الـ Logs

### مشكلة: خطأ في التحديثات
**الحل**:
- راجع `server/src/index.ts` - migrations
- تحقق من SQL syntax
- راجع الـ error logs

### مشكلة: لا يمكن الوصول للـ API
**الحل**:
- تحقق من CORS settings
- تحقق من environment variables
- تحقق من Railway deployment logs

---

## 📚 مراجع مفيدة:

- [Railway Volumes Documentation](https://docs.railway.app/reference/volumes)
- [Better-SQLite3 Documentation](https://github.com/WiseLibs/better-sqlite3)
- [Vite Deployment Guide](https://vitejs.dev/guide/static-deploy.html)

---

**النسخة**: 2.0  
**آخر تحديث**: 29 نوفمبر 2025  
**الحالة**: ✅ جاهز للإنتاج
