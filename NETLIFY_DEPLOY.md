# 🚀 دليل النشر على Netlify

## 📋 المتطلبات الأساسية

- ✅ حساب على [Netlify](https://app.netlify.com)
- ✅ حساب على [GitHub](https://github.com) (اختياري - للنشر التلقائي)
- ✅ Node.js النسخة 20 أو أحدث

---

## 🎯 الطريقة الأولى: النشر المباشر (سريع)

### 1️⃣ تثبيت Netlify CLI

```powershell
npm install -g netlify-cli
```

### 2️⃣ تسجيل الدخول

```powershell
netlify login
```

### 3️⃣ بناء المشروع

```powershell
cd web
npm install
npm run build
```

### 4️⃣ النشر

```powershell
# للنشر التجريبي
netlify deploy --dir=dist

# للنشر النهائي (Production)
netlify deploy --prod --dir=dist
```

---

## 🔄 الطريقة الثانية: النشر التلقائي من GitHub (مُوصى به)

### 1️⃣ رفع المشروع على GitHub

```powershell
# تهيئة Git (إذا لم يكن موجود)
git init
git add .
git commit -m "Initial commit"

# ربط مع GitHub
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git push -u origin main
```

### 2️⃣ ربط Netlify بـ GitHub

1. اذهب إلى [app.netlify.com](https://app.netlify.com)
2. اضغط **"New site from Git"**
3. اختر **GitHub** وأذن لـ Netlify بالوصول
4. اختر Repository الخاص بك
5. ستجد الإعدادات مُكتشفة تلقائياً من `netlify.toml`:
   - **Base directory:** `web`
   - **Build command:** `npm run build`
   - **Publish directory:** `web/dist`

### 3️⃣ إضافة متغيرات البيئة

في لوحة Netlify:
1. اذهب إلى **Site settings** > **Environment variables**
2. أضف المتغير التالي:
   - **Key:** `VITE_API_URL`
   - **Value:** `https://salary.soqiamakkah.com/api`

### 4️⃣ النشر

اضغط **"Deploy site"** - سيبدأ النشر تلقائياً!

---

## ⚙️ الإعدادات المُطبقة

تم تكوين `netlify.toml` مع:

### 🏗️ إعدادات البناء
- Node.js النسخة 20
- مجلد البناء: `web`
- أمر البناء: `npm run build`
- مجلد النشر: `web/dist`

### 🔀 إعادة التوجيه
- جميع المسارات تُعاد إلى `index.html` (دعم SPA)

### 🛡️ الأمان
- X-Frame-Options: DENY
- X-XSS-Protection: 1; mode=block
- X-Content-Type-Options: nosniff
- Referrer-Policy: strict-origin-when-cross-origin

### ⚡ التخزين المؤقت
- ملفات `/assets/*` تُخزن لمدة سنة

---

## 🔗 معلومات السيرفر

### السيرفر الحالي
```
URL: https://salary.soqiamakkah.com/api
الحالة: يعمل ✅
```

**ملاحظة:** السيرفر يعمل على خادم خارجي منفصل. الواجهة فقط على Netlify.

---

## ✅ التحقق من النشر

بعد النشر، تحقق من:

### 1. الصفحة الرئيسية
- [ ] الصفحة تحمل بدون أخطاء
- [ ] التصميم يظهر بشكل صحيح

### 2. تسجيل الدخول
- [ ] نموذج تسجيل الدخول يظهر
- [ ] يمكن الاتصال بالسيرفر
- [ ] تسجيل الدخول يعمل بنجاح

### 3. الصفحات
- [ ] لوحة التحكم تحمل البيانات
- [ ] المشاريع تظهر
- [ ] المصروفات تعمل
- [ ] الإحصائيات تظهر

### 4. النماذج
- [ ] إضافة مشروع جديد
- [ ] إضافة مصروف جديد
- [ ] التعديل يعمل
- [ ] الحذف يعمل

### 5. التصدير
- [ ] تصدير CSV يعمل
- [ ] البيانات صحيحة

---

## 🐛 حل المشاكل الشائعة

### ❌ خطأ: Failed to fetch

**السبب:** الواجهة لا تستطيع الاتصال بالسيرفر

**الحل:**
1. تأكد من أن `VITE_API_URL` مُعرّف في Environment Variables
2. تأكد من أن السيرفر يعمل: `https://salary.soqiamakkah.com/api/health`
3. تحقق من إعدادات CORS في السيرفر

### ❌ خطأ: Page not found عند التنقل

**السبب:** إعدادات SPA غير صحيحة

**الحل:** تأكد من وجود redirect في `netlify.toml`:
```toml
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

### ❌ البيانات لا تحمل

**السبب:** مشكلة في الاتصال بالسيرفر

**الحل:**
1. افتح Developer Console (F12)
2. تحقق من Network tab
3. إذا كانت الطلبات تذهب لـ `/api` بدلاً من `https://salary.soqiamakkah.com/api`:
   - تأكد من أن `VITE_API_URL` مُعرّف
   - أعد بناء ونشر المشروع

### ❌ Build fails

**السبب:** خطأ في البناء

**الحل:**
1. راجع Netlify Deploy Logs
2. جرب البناء محلياً:
   ```powershell
   cd web
   npm run build
   ```
3. إذا نجح محلياً، تأكد من Node version في Netlify (يجب أن يكون 20)

---

## 📱 اختبار على الأجهزة المختلفة

بعد النشر، اختبر على:
- [ ] كمبيوتر مكتبي (Chrome, Firefox, Edge)
- [ ] جوال (iOS Safari, Android Chrome)
- [ ] تابلت

---

## 🔄 التحديثات المستقبلية

### مع GitHub (تلقائي)
فقط:
```powershell
git add .
git commit -m "وصف التحديث"
git push
```
سيتم النشر تلقائياً! ✨

### بدون GitHub (يدوي)
```powershell
cd web
npm run build
netlify deploy --prod --dir=dist
```

---

## 📊 المراقبة

### Netlify Analytics
- زيارات الموقع
- معدلات التحميل
- الأخطاء

### رابط اللوحة
```
https://app.netlify.com/sites/YOUR_SITE_NAME/overview
```

---

## 🆘 الدعم

### مشاكل Netlify
- [Netlify Forums](https://answers.netlify.com/)
- [Netlify Docs](https://docs.netlify.com/)

### مشاكل المشروع
راجع الملفات:
- `README.md` - معلومات المشروع
- `DEPLOYMENT_GUIDE.md` - دليل النشر الشامل

---

## 🎉 تم بنجاح!

الآن المشروع يعمل على Netlify مع:
- ✅ نشر تلقائي (إذا استخدمت GitHub)
- ✅ SSL مجاني
- ✅ CDN عالمي
- ✅ نسخ احتياطية تلقائية
- ✅ معاينة التحديثات قبل النشر

**رابط الموقع:** سيكون على شكل `https://YOUR_SITE_NAME.netlify.app`

(يمكن تغييره لدومين مخصص من إعدادات Netlify)
